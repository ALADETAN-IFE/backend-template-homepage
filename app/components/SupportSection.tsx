// SupportSection.tsx
import React from "react";

const SupportSection = () => {
  return (
    <section id="support" className="py-24 px-4 bg-card/30 border-t border-border/50">
      {/* ...existing support section code from page.tsx... */}
    </section>
  );
};

export default SupportSection;
